<!DOCTYPE html>
<html lang="en">

<h1>Uncovering latent consensus in heterogeneous populations: The Mixture Linear Ordering Problem — Supporting Material</h1>

[![DOI](https://zenodo.org/badge/DOI/10.5281/zenodo.XXXXXXXX.svg)](https://doi.org/10.5281/zenodo.XXXXXXXX)

<p>This repository provides the supplementary material for the article:</p>

<p><strong><em>“Uncovering latent consensus in heterogeneous populations: The Mixture Linear Ordering Problem”</em></strong></p>

<p><strong>Authors</strong><br>
Juan A. Aledo – Universidad de Castilla-La Mancha (UCLM), Spain<br>
Concepción Domínguez – Universidad de Murcia (UM), Spain<br>
Juan de Dios Jaime-Alcántara – Universidad Miguel Hernández de Elche (UMH), Spain<br>
Mercedes Landete – Universidad Miguel Hernández de Elche (UMH), Spain
</p>

<hr>

<h2>📁 Repository structure</h2>

<pre><code>inputs/
models/
outputs/
info_synthetic/
README.md
</code></pre>

<ul>
  <li><code>inputs/</code>: input instances in <code>.dat</code> format, including the synthetic instances <code>R1, R2, ..., R36</code> and the sushi instance.</li>
  <li><code>models/</code>: Xpress/Mosel model files implementing the compact exact formulation and the heuristic approach presented in the article.</li>
  <li><code>outputs/</code>: output files generated by the models, together with an Excel summary of the obtained solutions.</li>
  <li><code>info_synthetic/</code>: text files containing additional information on the synthetic instances, including the group structure, rankings, weights, matrices, and objective values.</li>
</ul>

<hr>

<h2>📄 Format of the <code>.dat</code> files</h2>

<p>Each instance is provided in plain text format and contains:</p>

<ul>
  <li><code>n</code>: number of items</li>
  <li><code>m</code>: number of voters</li>
  <li><code>c</code>: an <code>n × n</code> matrix with aggregated preference intensities</li>
</ul>

<p>The general structure is:</p>

<pre><code>n: &lt;number_of_items&gt;
m: &lt;number_of_voters&gt;
c:
[
  &lt;row_1&gt;
  &lt;row_2&gt;
  ...
  &lt;row_n&gt;
]
</code></pre>

<hr>

<h2>📄 Format of the synthetic information files</h2>

<p>Each file in <code>info_synthetic/</code> is provided in plain text format and is organized by groups.</p>

<p>The general structure is:</p>

<pre><code>GROUP 1
Ranking
...
Weight
...
Matrix
...
Objective value: ...

GROUP 2
Ranking
...
Weight
...
Matrix
...
Objective value: ...

...

TOTAL OBJECTIVE VALUE: ...
</code></pre>

<p>For each group, the file contains:</p>

<ul>
  <li><code>Ranking</code>: ordering associated with the group</li>
  <li><code>Weight</code>: weight assigned to the group</li>
  <li><code>Matrix</code>: matrix associated with the group, with all entries multiplied by <code>1000</code></li>
  <li><code>Objective value</code>: objective value corresponding to that group</li>
</ul>

<p>The last line reports the aggregated objective value for the whole instance.</p>

<hr>

<h2>▶️ Using the models</h2>

<p>
The Mosel files in <code>models/</code> correspond directly to the formulations described in the article.
To run an instance:
</p>

<ol>
  <li>Open one of the <code>.mos</code> files in Xpress.</li>
  <li>Provide a <code>.dat</code> file from <code>inputs/</code>.</li>
  <li>Execute the model to obtain the corresponding solution and objective value.</li>
  <li>Inspect the generated outputs in <code>outputs/</code> and the additional synthetic information in <code>info_synthetic/</code>, when applicable.</li>
</ol>

<p>The meaning and purpose of each formulation are described in detail in the article.</p>

<hr>

<h2>📝 Citation</h2>

<p>If you use this material, please cite:</p>

<pre><code>@article{aledo2026uncovering,
  title   = {Uncovering latent consensus in heterogeneous populations: The Mixture Linear Ordering Problem},
  author  = {Aledo, Juan A and Dom{\'i}nguez, Concepci{\'o}n and Jaime-Alc{\'a}ntara, Juan de Dios and Landete, Mercedes},
  journal = {Preprint},
  year    = {2026}
}
</code></pre>

</body>
</html>
